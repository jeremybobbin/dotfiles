#!/bin/sh

# Outputs mac address of from bluetoothctl
device_name=$@
device_regexp="^.*$device_name.*$"
mac_regex='([[:xdigit:]]{1,2}:){5}[[:xdigit:]]{1,2}'

echo devices |
	bluetoothctl |
	grep -Eio "$device_regexp" |
	grep -Eio "$mac_regex"
