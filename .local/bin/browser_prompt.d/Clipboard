#!/bin/sh

url_grep() {
	grep -Eo "[^ \"\(\)\<\>]+\.[^ \"\(\)\<\>]+"
}

url=$(xclip -selection c -o | url_grep)
[ "$url" ] && $BROWSER $url && exit

url=$(xclip -selection p -o | url_grep)
[ "$url" ] && $BROWSER $url && exit
