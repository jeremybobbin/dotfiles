#!/bin/sh
methods="$XDG_BIN_HOME/extract.d"

if [ -z "$@" ]; then
	method=$(ls $methods | dmenu_color -i -p 'What to extract:')
else
	method="$@"
fi

script="$methods/$method"

( [ ! -e "$script" ] || [ -d "$script" ] ) && exit 1 

"$script" | unique | dmenu_color -l 10

