#!/bin/sh

[ $(id -u) == 0 ] ||
	echo "You must be root to run this script" &&
	exit 1 

openldap="/etc/openldap"

rm -rf "$openldap/slapd.d/*"
mkdir -p "$openldap/slapd.d/"
slaptest -f "$openldap/slapd.conf" -F "$openldap/slapd.d/"
chown -R ldap:ldap "$openldap/slapd.d"
systemctl restart slapd
