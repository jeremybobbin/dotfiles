#!/bin/sh

if [ $# = 0 ]; then
	rustup docs --std
	return 0
fi

toolchain="$HOME/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/"
query="file://$toolchain/share/doc/rust/html/std/index.html?search=$1"
(nohup "$BROWSER" "$query" > /dev/null 2>&1 &)
