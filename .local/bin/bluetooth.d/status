#!/usr/bin/sh

device_name="$1"

if bluetooth mac_connected "$(bluetooth get_mac "$device_name")"; then
	notify-send 'Bluetooth' "Currently connect to device: $device_name"
else
	notify-send 'Bluetooth' "No connections"
fi
