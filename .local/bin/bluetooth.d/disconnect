#!/bin/bash
bluetooth initialize

# Get mac address of AirPods from bluetoothctl
[ "$1" ] &&
	device_name="$1" || 
	device_name='AirPods'

mac_address="$(bluetooth get_mac "$device_name")"

notify-send 'Bluetooth' "Disconnecting from $device_name"

echo -e "disconnect $mac_address" | bluetoothctl

