#!/usr/bin/sh
bookmarks="$XDG_CONFIG_HOME/bookmarks"
prompt() {
	tac "$bookmarks" | unique | cut -f1 |
		dmenu -i -l 10 -p "Please select: "
}

chosen=$(prompt)

if [ "$?" -ne 0 ]; then
	exit 1
fi

$BROWSER "$(grep "$chosen" "$bookmarks" | cut -f2)"
