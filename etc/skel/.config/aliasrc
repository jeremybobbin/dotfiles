# Rust Related 
# Need to escape so that alias generator doesn't evaluate
ect	ct=$(ct) && e $ct


# Shell
o		"$XDG_BIN_HOME/open"
c		fasd_cd -d -i

ls		command ls --color=auto --group-directories-first -hN 
la		command ls --color=auto -ahN --group-directories-first
ll		command ls --color=auto -lhN --group-directories-first
lc		command ls --color=always -hN --group-directories-first 
lt		command ls --color=auto -thN 

new		($TERMINAL -c "cd $PWD" &>/dev/null & disown)

# Lolipop & Jenkins Operator
-o		xargs -n1
_		xargs -n1


# rm_wrapper function in ~/.bashrc
rm	rm_wrapper


sp	. ~/.profile
sbrc	. ~/.bashrc

ep	e ~/.profile
ebp	e ~/.bash_profile
exp	e ~/.xprofile

ebrc	e ~/.bashrc
eirc	e ~/.inputrc
exrc	e ~/.xinitrc
evrc	e ~/.vimrc
evrcd	e ~/.vim

emrc	e ~/.config/neomutt/neomuttrc
exhk	e ~/.config/sxhkd/sxhkdrc
earc	e ~/.config/aliasrc

# Administration
sc	systemctl
ssc	sudo systemctl
scu	systemctl --user

sscr	sudo systemctl restart
ssce	sudo systemctl enable
scur	systemctl --user restart
scue	systemctl --user enable

# Clipboard
xc	xclip -selection

cb	xc c
pcb	xc p
cbo	xc c -o
pcbo	xc p -o

cbimg	xc c -t image
pcbimg	xc p -t image

cbojpg	xc c -t image | jpg
pcbojpg	xc p -t image | jpg
cbopng	xc c -t image | jpg
pcbopng	xc p -t image | jpg

p2c	pcbo | cb
c2p	cbo | pcb

# Apps
g	git
se	sudo -E $EDITOR
n	nnn
z	zathura

# Make
m	make
smi	sudo make install
rh	runhaskell

# Media
rec	ffmpeg -video_size 1920x1080 -framerate 20 -f x11grab -i :0.0+0,0 "$HOME/Media/Video/ScreenCast/$(date +%s).mp4"
pm	pulsemixer

# Colors
ip	ip -c

grep	grep --color=auto 
yay	yay --color=auto
diff	diff --color=auto
pacman	pacman --color auto

grepc	grep --color=always 
diffc	diff --color=always
yayc	yay --color=always
pacmanc	pacman --color always

less	less -R
highlight	highlight --out-format=ansi 
less	less -R

mpv mpv --input-ipc-server="/tmp/mpv_$(date +%s%N)"

# Timers
25	termdown 25m 
5	termdown 5m

# Note
note	v ~/Notes/Self/$(date +%F)

# Networking
p8	ping 8.8.8.8
p42	ping 4.2.2.2
t8	traceroute 8.8.8.8
t42	traceroute 4.2.2.2
ipb	ip -br

# Mail
mutt	neomutt
m	neomutt


# Lambda
# Dialpad Number
dpn		echo 6502886100
# Lambda Dialpad Number
ldpn	echo 6504795530



# Temp
vps		echo 45.77.215.144

# Common Dir
uls	cd ~/.local/share
ulb	cd ~/.local/bin
cfg	cd ~/.config
cch	cd ~/.cache
dl	cd ~/Downloads
mda	cd ~/Media
doc	cd ~/Media/Documents
nts	cd ~/Notes
prj	cd ~/Projects
rs	cd ~/Projects/Rust
msc	cd ~/Media/Audio/Music
img	cd ~/Media/Images
vdo	cd ~/Media/Video
