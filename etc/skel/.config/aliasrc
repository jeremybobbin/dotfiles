# Shell
o		"$XDG_BIN_HOME/open"

la		/bin/ls --color=auto -ahN --group-directories-first
lc		/bin/ls --color=always -hN --group-directories-first 
ll		/bin/ls --color=auto -lhN --group-directories-first
ls		/bin/ls --color=auto --group-directories-first -hN 
lt		/bin/ls --color=auto -thN 

# Jenkins Operator
_		xargs


# rm_wrapper function in ~/.bashrc
rm	rm_wrapper

sp	. ~/.profile
sbrc	. ~/.bashrc

e	terminal $EDITOR
ebp	e ~/.bash_profile
ebrc	e ~/.bashrc
eirc	e ~/.inputrc
ep	e ~/.profile
evrc	e ~/.vimrc
evrcd	e ~/.vim
exp	e ~/.xprofile
exrc	e ~/.xinitrc

emrc	e ~/.config/neomutt/neomuttrc
exhk	e ~/.config/sxhkd/sxhkdrc
earc	e ~/.config/aliasrc

# Administration
sc	systemctl
scu	systemctl --user
scue	systemctl --user enable
scur	systemctl --user restart
ssc	sudo systemctl
ssce	sudo systemctl enable
sscr	sudo systemctl restart

# Clipboard
cb	xclip -selection c
pcb	xclip -selection p
scb	xclip -selection s

cb	xclip -selection c
pcb	xclip -selection p
scb	xclip -selection s

cbimg	xclip -selection c -t image -o
pcbimg	xclip -selection p -t image -o
scbimg	xclip -selection s -t image -o

c2p	cb -o  | pcb
c2s	cb -o  | scb
p2c	pcb -o | cb
p2s	pcb -o | scb
s2c	pcb -o | cb
s2p	pcb -o | pcb

# Apps
g	git
se	sudo -E $EDITOR
n	nnn
z	zathura

# Make
mk	make
smki	sudo make install
rh	runhaskell

# Media
rec	ffmpeg -video_size 1920x1080 -framerate 20 -f x11grab -i :0.0+0,0 "$HOME/Media/Video/ScreenCast/$(date +%s).mp4"
pm	pulsemixer

# Colors
catc	highlight --out-format=ansi 
diff	diff --color=auto
diffc	/bin/diff --color=always
grep	/bin/grep --color=auto 
grepc	/bin/grep --color=always 
ip	ip -c
less	less -R
pacman	pacman --color auto
pacmanc	pacman --color always
yay	yay --color=auto
yayc	yay --color=always

mpv mpv --input-ipc-server="/tmp/mpv_$(date +%s%N)"

# Timers
25	termdown 25m 
5	termdown 5m

# Note
note	v ~/Notes/Self/$(date +%F)

# Networking
p8	ping 8.8.8.8
p42	ping 4.2.2.2
t8	traceroute 8.8.8.8
t42	traceroute 4.2.2.2

ipash	ip address show
ipb	ip -br
ipbash	ip -br address show
ipblish	ip -br link show
ipbrosh	ip link show
iplish	ip link show
iprosh	ip link show

# Mail
mutt	neomutt
m	neomutt


# Lambda
# Dialpad Number
dpn	echo 6502886100

# Lambda Dialpad Number
ldpn	echo 6504795530

# Temp
vps		echo 45.77.215.144

# Common Dir
cch	cd ~/.cache
cfg	cd ~/.config
dl	cd ~/Downloads
doc	cd ~/Media/Documents
img	cd ~/Media/Images
mda	cd ~/Media
msc	cd ~/Media/Audio/Music
nts	cd ~/Notes
prj	cd ~/Projects
ulb	cd ~/.local/bin
uls	cd ~/.local/share
vdo	cd ~/Media/Video
