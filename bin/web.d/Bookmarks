#!/bin/sh
bookmarks="${XDG_CONFIG_HOME:-"$HOME/.config"}/bookmarks"
chosen=$(tac "$bookmarks" | unique | cut -f1 |
		dmenu -i -l 10 -p "Please select: ")

if [ "$?" -ne 0 ]; then
	exit 1
fi

grep "$chosen" "$bookmarks" | cut -f2
